rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // ✅ Allow each user to access only their own release artwork
    match /releaseArtwork/{userId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}